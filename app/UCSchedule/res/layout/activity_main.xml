<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btnGoToLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="101dp"
        android:text="@string/strLoginBtn" />

    <Button
        android:id="@+id/btnGoToCalendar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnGoToLogin"
        android:layout_alignBottom="@+id/btnGoToLogin"
        android:layout_alignParentRight="true"
        android:layout_marginRight="78dp"
        android:text="@string/strCalendar" />

    <Button
        android:id="@+id/btnGoToAddEvent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnGoToLogin"
        android:layout_alignBottom="@+id/btnGoToLogin"
        android:layout_centerHorizontal="true"
        android:text="add event (test)" />

</RelativeLayout>